# 让 AI 学会"用工具"，又管住它：一个 AI 原生 ERP 的工具系统设计

> 本文分享启禾 OS · 仓迹（一个 AI 原生进销存 ERP）在"AI 如何操作系统"这件事上的完整设计思路。
> 不贴代码，只讲思路——因为这些思路与你用什么语言、什么框架无关，任何想给 AI 接上"手脚"的项目都能用上。

---

## 一、缘起

先讲一个背景。

传统 ERP 的交互模型是：人打开菜单 → 找到表单 → 填字段 → 点保存。系统把用户训练成"会填表的操作员"。

我们想做一个反过来的东西：**人用自然语言说"帮张三下 10 个灭蚊灯的订单"，系统自己去找客户、查产品、算金额、建单、展示结果。** 人不需要学系统，系统应该懂人。

这个想法听起来很美，但做一个能"动手干活"的 AI 系统，真正难的不是大模型本身，而是三件事：

1. **AI 怎么"知道"它能做什么？** —— 系统有几十种能力，AI 怎么发现、怎么选、怎么正确调用？
2. **AI 怎么"干得对"？** —— 大模型会撒谎、会猜、会编参数，怎么保证每一次操作背后都是真实数据？
3. **怎么"管住" AI？** —— 这是最难的。AI 有了增删改查的权限之后，凭什么它不会乱删数据、不会越权操作、不会自己推进一个不该推进的业务状态？

这篇文章就是围绕这三件事展开的。全文基于我们真实踩过的坑和沉淀下来的设计，你可以把它当成一份"给 AI 接上工具系统"的实战手册。

需要说明的是：本文**只讲思路，不讲代码**。每个设计点我会解释"为什么这么做、解决什么问题、有什么代价"，但不会贴具体实现——因为实现会随项目演进而变化，而思路是稳定的。

---

## 二、项目介绍：启禾 OS · 仓迹

在展开技术细节之前，先花一分钟介绍这个项目本身。

**启禾 OS · 仓迹** 是一个 **AI 原生的进销存 ERP**。它的核心主张是：

> 这不是"ERP 系统加上 AI 功能"，而是"AI 作为操作系统的 ERP"。

什么意思？传统 ERP 的主界面是菜单和表单；仓迹的主界面是**对话**。你像和同事聊天一样告诉它你要做什么，它理解意图、调用工具、执行操作、返回结果。传统操作台（表格、按钮、看板）同时保留，作为高频作业的辅助入口。

它有四个核心差异化：

- **对话即 UI**：增删改查通过自然语言完成。"给张三下 10 个灭蚊灯的订单"——AI 自动完成查客户、查产品、算金额、建单、展示。
- **建表即上线**：新增业务能力不需要改前端。数据库里新建一张表，AI 通过工具自动发现并学会操作。业务扩展的成本从"前后端都改"降为"只加一张表"。
- **AI 不能编造数据**：任何涉及业务数据的回答，AI 必须先调用工具获取真实结果。不存在"AI 幻觉出幻数据"的可能。
- **知情同意**：不可逆操作（创建、修改、删除、批量导入）必须先弹出确认卡片，用户看清楚"涉及什么、影响什么"、点击确认后才真正落库。AI 不能静默删改数据。

功能上覆盖进销存的完整业务域：产品、库存、扫码作业、订单、采购、客户、退货、代发、运单出库、收支记账、待办看板、导入导出、Excel 报表引擎、权限与多租户等。仓库一线人员可以用手机扫码作业、断网也能出库；老板可以出差途中让 AI 生成一份欠款报表。

从技术上，它是一个前后端分离的应用：前端是 Vue 3，后端基于 PocketBase（一个开源的实时后端平台，提供数据库、鉴权、REST API、实时订阅和钩子），AI 部分基于 Eino（字节跳动开源的一个 Go 语言 Agent 编排框架）。

如果你想了解更多，可以访问项目官网：[https://www.qihebook.cloud](https://www.qihebook.cloud)。

接下来的内容，就是支撑上面这些能力背后的"AI 工具系统"设计。

---

## 三、核心命题

在讲工具系统之前，先立三个贯穿全文的命题。所有设计都从这里推导出来。

### 命题一：自然语言是操作系统的界面

传统系统里，界面是表单；在 AI 原生系统里，界面是对话。这意味着系统必须能**听懂意图**（自然语言理解）并且**执行动作**（工具调用）。听懂只是前提，执行才是主体——一个只会聊天不会干活的 AI，对业务系统毫无价值。

### 命题二：AI 不能编造数据

这是我们对 AI 最基本、最不可妥协的一条纪律。任何涉及业务数据的回答，AI 必须先调用工具拿到真实结果，再基于结果回答。库存、订单、客户、金额——背后必须是真实数据库查询的结果。

"不能编造"说起来容易，做起来难。因为大模型的默认行为就是"顺着话编一个合理答案"。要做到"不编造"，靠的不是在提示词里写一句"请不要编造"，而是：

- 把"先查再说"变成强制性的工具调用规则；
- 让工具返回的结构化结果直接进入对话上下文，成为 AI 回答的唯一事实来源；
- 从人格定义到技能文档、到代码硬拦截，每一层都重申并执行这条纪律。

### 命题三：工具是 AI 与数据世界的唯一桥梁

我们把所有对数据的操作都封装成**工具**。AI 不直接读写数据库，它只能：

```
调用工具 → 工具执行 → 返回结构化结果 → AI 基于结果继续
```

这个"唯一桥梁"的设计带来三个巨大的好处：

1. **安全可控**：所有操作都经过同一道闸门，可以在闸门上做权限校验、确认拦截、审计记录。
2. **AI 可学**：工具就是 AI 的能力清单，工具的定义就是 AI 的学习手册。
3. **数据保真**：AI 永远拿不到编造的机会——它回答的任何数据都来自工具的真实返回。

---

## 四、总体架构

先看一张概念图，理解工具系统在整个架构里的位置：

```
┌─────────────┐   自然语言    ┌──────────────────────────────┐
│   前端       │ ────────────▶ │        对话流接口              │
│ （Vue/PWA）  │ ◀──────────── │  （流式返回，逐字输出）          │
└─────────────┘    SSE 流      └──────────────┬───────────────┘
                                              │
                                              ▼
                               ┌──────────────────────────────┐
                               │         Agent 循环            │
                               │  （理解意图 → 决定调哪个工具 →  │
                               │    执行 → 看结果 → 继续/结束）   │
                               └──────────────┬───────────────┘
                                              │ 工具调用
                                              ▼
                               ┌──────────────────────────────┐
                               │         工具执行层             │
                               │  权限校验 → 确认拦截 → 审计 → 执行 │
                               └──────────────┬───────────────┘
                                              │
                                              ▼
                               ┌──────────────────────────────┐
                               │      工具执行进程（独立 CLI）     │
                               │  stdin 收参数 / stdout 回 JSON  │
                               └──────────────┬───────────────┘
                                              │ REST / 直连
                                              ▼
                               ┌──────────────────────────────┐
                               │         数据 API + 数据库        │
                               │  行级权限规则 + 触发器 + 状态机   │
                               └──────────────────────────────┘
```

每个环节的职责一句话：

| 层 | 职责 |
|----|------|
| 前端 | 展示对话、流式渲染、弹确认框、渲染结果卡片 |
| 对话流接口 | 接收用户消息，以 SSE 流式把 AI 的思考、工具调用、结果逐步推给前端 |
| Agent 循环 | 大模型推理：理解意图、决定调用哪个工具、消化工具结果、组织回答 |
| 工具执行层 | 对每一次工具调用做"权限 → 确认 → 审计 → 执行"的拦截链 |
| 工具执行进程 | 独立于主服务的进程，真正执行工具逻辑，与数据 API 交互 |
| 数据 API + 数据库 | 最终的数据读写，含行级隔离规则、业务钩子、状态机 |

有一个很重要的设计原则贯穿整个架构：

> **AI 只做平台做不到的事。** 大模型推理、意图理解、流式输出、工具编排——这些是 AI 独有的；而权限、数据校验、状态流转、事务一致性——这些交给平台层（数据库规则 + 钩子）去做，不靠 AI"自觉"。

后面所有章节，都是围绕"工具层"这个核心展开的。

---

## 五、支柱一：工具元数据单一事实源

### 5.1 问题

一个 AI 工具系统，第一件事是定义"工具有哪些、每个工具长什么样"。如果工具的清单和描述散落在各处（代码里一份、帮助文档里一份、提示词里一份），它们迟早会漂移——AI 看到的名片和实际执行的行为对不上，AI 就会选错工具。

### 5.2 设计

我们把**全部工具的定义收敛到一张集中表**。每个工具的定义包含：

- **名称**：AI 调用时用的唯一标识；
- **摘要**：一句话（≤30 字），注入系统提示词，让 AI 快速扫一眼知道"有哪些工具可用"；
- **完整描述**：什么时候用、什么时候不用、怎么用、带示例，通过帮助接口按需查询；
- **参数**：每个参数的名字、类型、是否必填、默认值、说明；
- **危险标记**：是否会改变数据（决定后续的确认和重试策略）；
- **隐藏标记**：只对内部流程暴露、不让 AI 看到的工具（比如保存聊天记录、加载历史这类内部工具）。

### 5.3 为什么这样设计

- **单一事实源**：工具目录、帮助文档、权限映射、参数校验——全部从这一张表派生。新增一个工具 = 在这张表加一行；永远不会出现"文档说有这个工具但实际没有"的情况。
- **提示词经济性**：系统提示词是稀缺资源。每个工具只放"摘要"（≤30 字），完整描述按需查询。几十个工具在提示词里只有几行，AI 想知道细节时再查帮助——这比把全部工具的长篇描述塞进提示词节省大量 token，也避免 AI 被巨量描述搞糊涂。

这套"摘要进提示词、详情按需查"的思路，是整个工具系统体验的关键基础。后面"AI 如何操控工具"一章会展开讲 AI 是怎么"按需查"的。

---

## 六、支柱二：两种工具形态

工具不是越多越好。我们把工具收敛成两种形态，各有各的适用场景。

### 6.1 宏工具：一个动作一件事

一个工具 = 一个不可再分的动作，参数少、语义明确。例如"创建记录""删除记录""描述表结构"。

适用场景：动作本身不可分解，或者分解成多步反而增加 AI 的调用轮次和出错概率。

### 6.2 脚本语言工具：一次传一串原子指令

有一类需求是**多步组合**：比如"查询客户 → 过滤欠款 > 0 的 → 按金额求和 → 排序 → 取前 10"。如果每一步都是一个工具，AI 要来回调用五轮，每轮都是 token 和延迟的浪费，而且中间状态容易丢。

我们的做法是：**CLI 即脚本语言**。把一类能力设计成"一门小语言"，AI 一次传一串有序的原子指令（steps），系统顺序执行：

```
[{op: from, collection: 客户表, filter: 欠款>0},
 {op: aggregate, func: sum, field: 金额, as: 欠款合计},
 {op: sort, by: 欠款合计, order: desc},
 {op: limit, n: 10}]
```

两个典型的脚本语言工具：

- **查询语言**：`from（拉数据）→ search（模糊/精确搜索）→ group（分组）→ aggregate（聚合）→ sort（排序）→ limit（限量）→ select（选列）`。AI 像拼积木一样编排查询，每一步都是确定性计算。
- **Excel 构建语言**：`create（建表）→ write_header（写表头）→ write_row（写行）→ data_import（从数据库拉数据）→ style（美化）→ save（保存并返回下载链接）`。AI 说"生成一份客户欠款报表"，它自己会把任务编排成一串原子指令提交，系统纯内存执行、零外部依赖。

### 6.3 脚本引擎的公共设计

两种脚本语言共享同一个引擎设计：

- **op 文档长在实现旁边**：每个 op 的名字、参数、示例就写在它的实现代码旁边，系统启动时自动汇总成完整语言手册。AI 查帮助时能看到每种语言的全套 op、规则和可复制的示例。
- **预校验 + 拼写纠错**：执行前先校验所有 op 名字，未知 op 报错并给出"编辑距离最近"的建议（AI 把 sort 打成 sotr，系统会说"你是不是想用 sort？"）。
- **资源上限**：步数上限、整段脚本超时上限。防止 AI 一次提交 10 万步把系统拖死。
- **原子语义**：任一步失败，整段脚本失败。要么全部成功，要么全部不执行——绝不允许"执行了一半"的脏状态。
- **定位信息**：失败时返回"执行到第几步、哪一步失败、为什么失败"，AI 可以只修正那一步后整体重试。

### 6.4 为什么两种形态

这背后是一条铁律：**工具数量是 LLM 的认知负担。** 每多一个工具，AI 选错工具的概率就上升一分。用"脚本语言"吸收掉所有多步组合类需求，工具数量就能保持收敛——我们曾经从 20 个工具收敛到 18 个，靠的就是把"查单条/查列表/聚合/排序"合并成一个查询语言工具，而不是新增三个工具。

---

## 七、支柱三：工具执行进程架构

### 7.1 为什么工具要"独立进程"执行

工具逻辑没有跑在主服务进程里，而是跑在一个**独立的 CLI 进程**里。为什么？

1. **故障隔离**：工具执行（比如解析一个几十 MB 的 Excel、跑一段复杂的聚合）如果崩溃或卡死，只影响这个子进程，不拖垮主服务。主服务给子进程设超时，超时直接杀掉重启。
2. **语言无关**：CLI 进程是一个黑盒接口。理论上任何语言都能实现工具，只要遵循同样的 stdin/stdout 协议。我们全用 Go，但架构上并不限定。
3. **进程级权限边界**：工具进程不需要持有主服务的全部权限，它只拿着"当前用户"的凭据做该做的事。

### 7.2 通信协议：JSON in, JSON out

工具进程的通信协议非常简单：

- **入参**：通过 stdin 传入（一次性模式）或逐行传入（常驻模式）。参数是 JSON。
- **出参**：通过 stdout 输出**一行 JSON**，结构统一（成功与否 + 结构化数据 + 错误信息 + 错误分类 + 下一步建议）。
- **stderr 纪律**：stderr 只放调试日志，绝不参与结果输出。

这个"stdout 一行 JSON"的纪律看起来琐碎，却救过我们一次命：早期版本把错误信息写到 stderr，结果在日志混流的环境下，结果解析器把日志当成了 JSON，导致 AI 拿到一堆解析失败的错误——这是"AI 工具系统"里最隐蔽也最致命的一类 bug。

### 7.3 常驻模式（--serve）

一次调用 fork 一个进程是有开销的（进程创建 + Go 运行时初始化 + 连接池重建）。为了压这层开销，我们做了常驻模式：

- 主服务维护一组**常驻 worker 进程**（一个连接池）；
- 通过 stdin 逐行发命令（一行一个 JSON：命令名 + 身份 + 参数），从 stdout 逐行读结果；
- 用完的进程不销毁，回池复用。

这样单次工具调用的开销从"毫秒级进程创建"降到"微秒级 IO"，AI 在多轮对话里连续调用几十次工具也没压力。

### 7.4 超时分级

不同工具的执行时长差异巨大：查一条记录是毫秒级，生成一份复杂 Excel 是秒级，批量导入运单可能是分钟级。所以超时不是一刀切，而是分级：

- 普通工具：默认 60 秒；
- 长任务工具（批量导入等）：放宽到 10 分钟。

教训来自一次事故：早期所有工具统一 60 秒超时，结果批量导入做到一半被超时杀掉，用户对着"导入失败"一脸懵，数据却已经写了一部分——超时不是越大越好，也不是越小越好，要按工具性质分级，配合原子性设计（见支柱二）才不会留脏数据。

---

## 八、AI 如何操控工具：发现—选择—调用—纠错闭环

AI 使用工具不是一个动作，是一个完整的闭环。每一环都有对应的设计。

### 8.1 发现：帮助目录按需查询

AI 怎么知道系统有哪些工具、每个工具怎么用？

- 系统提示词里只放每个工具的**一句话摘要**（见支柱一），让 AI 知道"有这些能力"。
- 想要细节时，AI 可以调用帮助系统：查工具列表、按意图搜工具、查某个工具的完整用法、查脚本语言的 op 表和示例。

这套帮助系统是**动态生成的**——工具定义变了，帮助自动变，不需要人维护文档。AI 永远不会拿着过时的说明书。

### 8.2 选择：技能文档教 AI 先选对工具

工具选错了，再好的执行也没用。我们通过**技能文档**来教 AI 做选择。

技能（Skill）是一份份存储在数据库里的业务知识文档，比如"订单处理""库存盘点""客户分析""Excel 报表"。每份技能文档会写明：

- 这类任务用什么工具、按什么顺序调；
- 什么情况**不要**用哪个工具（避免 AI 用错）；
- 典型示例。

AI 在对话中按需加载技能（用户可以用 `/技能名` 直接触发，也可以让 AI 自己判断需要时加载）。技能是**可读、可更新、可版本化**的知识——给 AI 加一个新能力，不需要改代码，写一份新的技能文档就行。

### 8.3 调用：参数校验 + 类型宽容

AI 调用工具时，最常见的错误是参数格式不稳定：明明定义了对象参数，它传了个字符串；明明要数组，它给了逗号分隔文本。

我们的策略是**系统侧宽容**：

- 服务端在工具执行前做一次**参数预校验**（必填是否齐全、类型是否合法），不合法直接返回结构化的"参数错误"给 AI 修正——绝不让错误参数进入业务逻辑；
- 对结构型参数（对象、数组）接受**两种形态**：原生 JSON 或 JSON 字符串。AI 给哪个都行，系统统一解析。

### 8.4 纠错：错误即教学

AI 调错工具、传错参数、踩中业务规则——几乎必然发生。关键不是"避免出错"，而是**让 AI 能自己纠错**。这就是下一章的主题。

---

## 九、错误即教学

这一章是整个工具系统里我认为最值得分享的设计。

### 9.1 核心认知：错误不是失败，是数据

传统系统里，错误是给开发者看的（堆栈、状态码、日志）；AI 系统里，错误是**给 AI 看的下一步指令**。

大模型没有"调试能力"，它只有文本上下文。当工具失败时，它唯一的行动依据就是工具返回的那段错误文本。所以错误信息的设计标准是：

> **错误信息要能让 LLM 自纠** —— 说清楚"为什么失败、该换成什么做法、下一步怎么办"。

### 9.2 统一的结果契约

所有工具返回统一结构：

```
- success   是否成功
- data      结构化结果数据
- error     给 AI 看的自然语言错误说明（为什么 + 怎么做）
- error_category  机器可读的错误分类（给编排层做决策用）
- suggestion     给 AI 的下一步行动建议
```

成功时 AI 拿 `data`；失败时 AI 拿 `error + suggestion` 决定怎么修正重试。编排层（比如重试、熔断、审计）拿 `error_category` 做机器决策——分类不是给人看的，是给系统看的。

### 9.3 把"AI 的常见错误"翻译成定向提示

这是最有价值的部分。我们在系统里预判了 AI 会犯的典型错误，并针对性地设计错误信息：

| AI 的典型错误 | 系统的回应 |
|--------------|-----------|
| 工具名拼错 | "未知工具，你是不是想用 xxx？"（编辑距离建议） |
| 必填参数缺失 | 列出缺了哪个，并提示查帮助 |
| 把"业务编号"当成记录 ID 传给关联字段 | 直接拦截："你传的是单号，需要传创建时返回的记录 ID"，并教正确写法 |
| 把排序误写成独立 op（`{op: order}`） | "没有 order 这个 op——order 是 sort 的参数，不是独立 op"，并给出 `{op: sort, by: ...}` 的改写示例 |
| 日期字段按直觉写 `= '2026-07-29'` 却查不到 | 系统把纯日期比较自动改写为时间区间，并说明原因 |
| 步骤脚本第 N 步失败 | "执行到第 N 步失败，失败 op 是 xxx，原因是……检查该步参数后整体重试" |
| 想推进订单状态（AI 不允许） | 明确告知"AI 不能推进业务状态"，并给出替代路径（创建草稿，由用户在操作台确认） |

这些错误信息不是一拍脑袋写的，它们来自真实用户和 AI 的对话日志。每一条都回答三个问题：**错在哪、为什么错、怎么办**。

### 9.4 分层：错误给 AI，分类给系统，审计留痕

- 自然语言错误 → AI 读取并自纠；
- 机器分类 → 编排层决策（比如"参数类错误不重试、网络类错误自动重试"）；
- 完整原始错误 → 进审计日志，供人工排查。

这样同一个失败事件，AI、系统、人都拿到了各自需要的信息。

---

## 十、三层约束模型：怎么限制 AI 做下一步

现在进入全文最核心的章节。AI 有了工具之后，凭什么不乱来？

我们的答案是**三层约束模型**。每一层回答一个不同的问题：

| 层 | 载体 | 回答的问题 | 性质 |
|----|------|-----------|------|
| 第一层：提示词/人格层 | 人格定义 + 技能文档 | AI **应该**怎么做 | 软约束，靠模型自律 |
| 第二层：工具契约层 | 结果契约 + 确认号协议 | AI 做错了**如何被纠回** | 半硬约束，靠错误驱动自纠 |
| 第三层：代码硬拦截层 | 权限中间件 + 数据规则 + 熔断 | 哪些动作 AI **永远不被允许** | 硬约束，不依赖模型自觉 |

### 10.1 第一层：提示词/人格层（教）

把行为准则写进 AI 的"人格"和技能文档。比如：

- **不编造数据**：涉及业务数据的回答必须先调用工具；
- **先查后答**：不知道就查，不猜；
- **知情同意**：不可逆操作先确认；
- **连续失败停止**：同一工具连续失败多次，停止这条链，向用户说明，而不是死磕；
- **信息不足先追问**：用户意图不完整时先问，不自行猜测推进。

这一层的优点是可读、可迭代（改文档就能改行为）；缺点是**靠模型自律，一定会被违反**。所以它只是第一层，不是全部。

### 10.2 第二层：工具契约层（纠）

当 AI 违反规则时，工具返回的结构化错误把它拉回正轨（见上一章）。这层的关键是"错误必须可执行"——AI 拿到错误信息后，知道下一步该做什么。

确认号协议也在这层：高危工具要求先拿"确认号"才能执行，没有确认号，工具直接拒绝并提示正确流程。

### 10.3 第三层：代码硬拦截层（拦）

这是真正的防线，不依赖任何提示词。所有工具调用都经过一道拦截链：

```
参数预校验 → 权限校验 → 确认门禁 → 审计 → 重试策略 → 执行
```

逐个展开：

**① 权限校验**：每个工具映射到权限点（读权限/写权限/管理权限），按用户角色（创始人/管理员/员工）拦截。无权限直接拒绝，根本不进业务逻辑。权限点映射是显式维护的——新增工具时，必须同时声明它需要的权限。

**② 系统表黑名单**：一组内部表（用户、审计日志、会话、确认记录等）对 AI 完全隐藏——AI 连"看"都看不到，更别说操作。隐藏不是靠提示词"不要动这些表"，而是工具层直接拒绝。

**③ 多租户隔离**：租户标识从请求一路注入到每一次写操作，AI 不传、也不能传（见第十二章）。

**④ 集中写策略**：不是所有"更新"都允许。一套集中的写入规则约束 AI 能改什么。这套规则可以浓缩成一句话：

> **AI 永远不能推进业务状态：只能创建草稿，只能删除已取消/已拒绝的终态记录——其余一切状态变动，由人在操作台完成。**

拆开来看：

- **只能创建草稿**：创建单据时，状态字段一律被拒绝——AI 新建的任何单据都只能是草稿态。AI 想"下单""发货""完成"，它做不到，只能建好草稿，等用户在操作台推进。
- **只能删除终态**：删除同样受限——只有"已取消/已拒绝"这类终态（以及草稿态）的记录允许 AI 删除；存续中的业务单据 AI 碰不了。这样即使"删错了"，影响也被限制在已终结的业务里。
- **不能推进状态机**：订单的"草稿→已确认→已发货"，采购的"草稿→已下单→已收货"——任何带状态字段的更新都会被直接拒绝。AI 可以编辑非状态字段（改客户名、改数量），但不能替人做业务决策。
- **受保护字段**：状态类字段 AI 一律不许改，连创建时都不许带。
- **父记录校验**：明细表的写入必须校验父记录存在、且属于当前租户。
- **关联字段必须传 ID**：AI 想关联一条记录，必须传系统生成的记录 ID，传"业务编号"会被拦截（见错误即教学一章）。

**⑤ 确认门禁**：高危操作强制确认（见下一章）。

**⑥ 连续失败熔断**：同一工具连续失败达到上限后，该工具暂时熔断，返回"此路不通，换其他方式或终止任务"——防止 AI 在死循环里反复调用同一错误工具浪费 token。

**⑦ 迭代步数上限**：整个对话的推理步数有上限，超出即终止。防止 AI 陷入无限工具调用循环。

**⑧ 上下文超限自动降级**：AI 的上下文窗口满了，不是拒绝任务，而是自动把大内容落盘、用占位符替换，AI 需要时按需读回（见第十三章）。

### 10.4 三层怎么配合

用一个例子串起来：

用户说"把订单 NO.20260715001 取消"。

1. **第一层**：AI 的人格要求它"不可逆操作先确认"，技能文档教它"先查订单 → 调确认工具 → 等用户确认 → 再执行"。
2. **第二层**：AI 如果跳过确认直接调"更新订单"，工具契约会拒绝："此操作需要确认"，并告诉它正确流程。
3. **第三层**：即使 AI 拿了确认号、也改了参数，硬拦截层还会检查：改 status 是**状态机推进**，AI 永远不被允许——拒绝并提示"请到操作台完成取消"。即使取消操作在业务上被允许，人也必须在界面上亲手完成。

每一层都可能拦住 AI，但拦截的语义不同：第一层是"劝"，第二层是"纠"，第三层是"锁"。缺了任何一层，系统都不安全；只有第三层，系统会难用（AI 动辄被拒，无法自纠）；只有第一二层，系统会失控。

---

## 十一、人机确认机制（confirm）

确认机制是"限制 AI"里最精细、也最容易做砸的部分。做砸的典型样子是：AI 疯狂弹确认窗烦死用户，或者确认窗形同虚设。我们的设计目标是：**关键操作必须人拍板，但确认流程的摩擦要最小化。**

### 11.1 基本流程

1. AI 判断一个操作是高危的（创建/修改/删除/批量导入）→ 先调用"确认"工具，提交一份确认请求（做了什么、影响哪些记录、严重程度）；
2. 前端弹出确认卡片，用户看到"涉及哪张表、哪条记录、什么变化"；
3. 用户点"确认执行"→ 系统给 AI 发放一个**确认号**（5 分钟有效）；
4. AI 拿确认号去执行真正的操作，工具校验确认号有效后才放行；
5. 用户也可以点"拒绝"→ AI 收到拒绝信号，放弃操作。

### 11.2 四个精妙细节

**① 确认号可批量复用**。一次确认号在 5 分钟内可以多次使用。为什么？因为一个业务动作往往拆成多条记录写入（一个采购单 = 采购头 + 明细 + 关联支出）。如果每条都要单独确认，AI 会弹三次窗，用户会疯。让 AI 确认一次、批量执行同一批操作，既安全又高效。

**② 确认工具必须单独一轮**。AI 在一轮里可能同时调用多个工具。如果"确认工具"和其他工具一起调用，系统会**剥离其他工具，只执行确认**。为什么这么苛刻？因为同轮并行时，其他工具的参数如果被上下文压缩丢失，AI 重建参数时可能执行错了对象——用户确认了"A 操作"，实际执行的却是 B。确认的意义就没了。宁可让 AI 多跑一轮，也要保证"确认什么就执行什么"。

**③ 确认上下文不被清理**。长对话里上下文会被压缩清理（见第十三章）。但确认相关的记录**豁免清理**——如果 AI"失忆"了，它会反复弹确认窗骚扰用户。保留这段上下文，AI 就知道"我已经确认过了，直接带确认号执行"。

**④ 用户一句话就能放行或终止**。确认后用户说"确认/执行吧"→ AI 直接执行，不再重新确认一遍；用户说"算了/取消"→ AI 放弃。门禁是给人用的，不是流程的装饰品。

### 11.3 确认状态机

确认请求有完整生命周期：待确认 → 已确认 / 已拒绝；已确认的请求在有效期内可复用，过期自动失效；超时未处理的孤儿请求由后台任务清理。所有确认动作写入审计，谁确认的、什么时候、确认了什么——全程可追溯。

---

## 十二、多租户隔离：AI 无需感知

多租户系统的噩梦是数据串租户——A 公司的 AI 把 B 公司的订单改了。我们的隔离设计有个特点：**AI 完全不需要知道租户这件事。**

### 12.1 全链路注入

- 用户登录时，系统解析出他所属的租户标识；
- 每次工具调用，这个标识从请求 → 工具适配器 → 执行进程 → 数据 API，一路透传；
- 写操作时，系统**无条件覆盖**写入租户标识——AI 传了也没用，系统永远用自己的；
- 读操作时，查询条件**自动拼接**租户过滤——AI 手写的过滤条件里不需要、也不允许写租户条件。

### 12.2 数据库层兜底

即使 AI 层漏了，数据库层还有最后一道防线：数据表的访问规则强制要求"记录的租户 = 当前用户的租户"。这条规则是数据平台级的能力，不依赖任何应用代码——即使有人绕过 AI 直接调 API，也读不到其他租户的数据。

### 12.3 为什么 AI 要"无知"

如果让 AI 感知租户，它就会犯两种错：漏写租户条件（读到别人数据）、写错租户条件（读不到自己数据）。把租户从 AI 的认知里彻底移除，是一种"少即是多"——系统的复杂性由平台层消化，AI 只关注业务本身。

---

## 十三、上下文经济性：AI 的记忆是稀缺资源

大模型的上下文窗口有限，而工具系统的每一次调用都在消耗它。上下文管理做不好，系统会在长对话中慢慢"变笨"。我们的原则是：

> **永不拒绝，只降级。** 上下文再满，也不能让任务中断——把内容降级处理，任务继续。

### 13.1 从源头节约

- 查询工具默认只返回小结果集（比如 50 行），不一次性拉全表；
- 超过阈值的大结果**自动瘦身**：只保留关键列，AI 需要完整字段时显式要求；
- 更大的结果（比如导出 1 万行）**落成文件**，返回下载链接——AI 的上下文里永远只放链接，不放内容。

### 13.2 超限时降级

- 单条工具结果超过阈值（比如 8K 字符）→ 落盘，上下文里留一个占位符（含路径和大小），AI 需要时用"读回"工具按需分页读取；
- 累计上下文接近上限 → 把最早的工具调用参数和结果清理落盘，**保留最近几轮**；
- 关键内容豁免清理：确认相关的上下文（防止 AI 失忆重复弹窗）、读回工具自己的结果（防止读回内容又被落盘的死循环）。

### 13.3 为什么"降级"而不是"拒绝"

拒绝 = 任务中断，用户体验断裂；降级 = 任务继续，只是 AI 需要多一步"按需读回"。我们把"记住一切"的负担从模型身上拿掉，交给确定性系统（落盘、文件、链接），模型只在需要时才取回。这让长会话、大数据场景都变得可行。

---

## 十四、工具演进方法论

工具系统不是一次建完的，它会随着业务演进而不断增减。我们沉淀了一套方法论来管理这个过程。

### 14.1 三条铁律

**反复杂**：能用"脚本语言"扩展已有工具的能力，就不新增工具。工具数量是 LLM 的认知负担，每多一个工具，选错工具的概率就上升。我们曾经 20 个工具，通过合并收敛到 18 个，能力反而更强了。

**反架构债务**：废弃的工具**直接移除**，不做"隐藏式软下线"、不保留兼容层。AI 没有"使用习惯"一说——改一份技能文档就完成了迁移。留着死工具只会让帮助目录越来越臃肿，让 AI 越来越容易选错。

**单一事实源**：工具清单只维护一份（见支柱一）。帮助、权限、校验全部从它派生，绝不在第二处维护工具清单。

### 14.2 新增工具的检查清单

1. 先问：能不能用脚本语言扩展已有工具？不能才新增；
2. 定义工具元数据（摘要 ≤30 字、描述含"何时用/何时不用" + 示例）；
3. 声明权限映射（这个工具需要什么权限）；
4. 声明危险等级（是否高危、是否要确认）；
5. 写技能文档教 AI 怎么用；
6. 全链路测试 + 更新工具数量统计。

### 14.3 移除工具的检查清单

1. 移除工具定义和实现；
2. 从权限映射、审计分类、常量清单里同步移除；
3. 改写技能文档，把调用示例替换为替代写法；
4. 全链路 grep 确认无残留引用；
5. 测试 + 部署 + 冒烟。

这套方法论让工具系统在长期演进中保持"小而精"，而不是"大而乱"。

---

## 十五、与 Agent 框架的集成

工具系统最终要挂在 Agent 框架上跑。我们经历了从"手写循环"到"框架化"的演进，分享几个关键认知。

### 15.1 手写循环 vs 框架

早期版本是手写的 ReAct 循环（模型生成 → 执行工具 → 结果回填 → 再生成），后来迁移到开源的 Agent 编排框架（Eino ADK）。框架接管了循环控制、流式输出、上下文管理这些通用能力，我们只保留业务相关部分（工具层、确认协议、SSE 协议）。

**关键原则：框架负责通用，业务层负责特化。** 工具执行层（权限/确认/审计）和对外协议（流式帧）是我们的业务资产，框架不该碰；循环、重试、模型切换这些通用机制，交给框架。

### 15.2 模型故障转移

AI 能力依赖大模型服务，而模型服务可能限流、可能挂。我们的策略是配置一个**模型链**：主模型挂了/限流时，自动切换备选模型。切换不是简单的"换一个"，还涉及成本感知（优先切便宜的后备）和恢复（主模型恢复后自动切回）。

### 15.3 确认的两条技术路线

人机确认在 Agent 框架里有两条实现路线，我们在实践中有明确取舍：

- **路线一（当前用）**：确认工具执行后**直接终结当前推理轮**，等用户确认；用户确认后，AI 带着确认号在下一轮重试原操作。简单、稳定。
- **路线二（未来优化）**：使用框架的"挂起/恢复"能力——确认工具执行时把整个会话状态挂起存档，用户确认后**恢复同一个会话**继续执行。体验更好（AI 不会"失忆"），但依赖框架的 checkpoint 机制，复杂度更高。

**建议：先用路线一跑通，体验可接受；等确认流程成为瓶颈时再上路线二。** 别一开始就追求完美架构。

### 15.4 SSE 流式协议

前端要实时看到 AI 的思考过程、工具调用过程、确认弹窗。我们定义了若干种流式帧：文本 token、信息提示、工具调用开始、工具结果、确认请求、错误、会话结束。前端按帧类型渲染——这是"对话即 UI"的底层支撑。

---

## 十六、可带走的蓝图

如果你也想在自己的项目里给 AI 接上工具系统，这是我们的建议。

### 16.1 最小可行组合

别一上来就做全套。先做这五件事，就能跑通"AI 安全地操作系统"的主干：

1. **工具元数据表**：一个集中的工具定义（名称/摘要/参数/危险标记）——这是一切的起点；
2. **统一结果契约**：所有工具返回同一结构（成功/数据/错误/分类/建议）——这是 AI 自纠的基础；
3. **权限门禁**：每个工具声明所需权限，执行前拦截——这是安全底线；
4. **确认门禁**：高危操作先确认、确认号再执行——这是"人拍板"的保证；
5. **上下文管理**：摘要进提示词、详情按需查、超限只降级不拒绝——这是长会话不劣化的保证。

### 16.2 决策点清单

- 工具形态：宏工具还是脚本语言？——多步组合优先考虑脚本语言；
- 执行位置：进程内还是独立进程？——独立进程换故障隔离和语言自由，代价是进程管理；
- 错误设计：错误给谁看？——永远记得错误是给 AI 看的，要可执行；
- 确认粒度：单条确认还是批量复用？——先想清楚业务动作的"一批"是什么；
- 权限粒度：工具级还是字段级？——先工具级，字段级按需再上；
- 框架选型：手写还是框架？——通用部分用框架，业务部分自己写。

### 16.3 最重要的三件事

如果只记三句话：

1. **工具是 AI 与数据的唯一桥梁**——所有操作走同一道闸门，安全、审计、确认都在这道闸门上做；
2. **错误信息要能让 LLM 自纠**——错误不是终点，是给 AI 的下一步指令；
3. **关键权限用代码锁，不用提示词劝**——AI 会违反提示词，但突破不了代码。

---

## 十七、踩过的坑

最后分享一些真实的教训。每一条都曾让我们付出过代价，也都在系统里留下了对应设计。

1. **统一超时误杀长任务**：早期所有工具统一 60 秒超时，批量导入做一半被杀，留下半脏数据。→ 超时按工具性质分级（见 7.4）。
2. **stderr 与结果混流**：错误信息写进 stderr，与日志混流后结果解析器拿到非 JSON，AI 拿到一堆"解析失败"。→ stdout 只输出结果 JSON，stderr 只放日志（见 7.2）。
3. **审计只记成功**：早期审计只记成功操作，失败操作（很可能是攻击或探测）无痕。→ 成功失败都记（见 10.3）。
4. **错误输出泄露内部信息**：工具失败时把完整内部错误（含路径、堆栈）返回给 AI。→ 对外输出截断、只保留给 AI 可执行的摘要（见 9.4）。
5. **孤儿工具调用记录**：确认后上下文被压缩，AI 忘掉自己调过确认工具，反复弹确认窗。→ 确认上下文豁免清理（见 11.2）。
6. **日期写法直觉不匹配**：AI 按直觉写 `日期 = '2026-07-29'` 永远查不到（数据库存的是完整时间戳）。→ 系统自动把纯日期比较改写为时间区间，并告诉 AI（见 9.3）。
7. **租户标识可被客户端伪造**：早期信任客户端传来的租户 ID。→ 服务端用成员关系重新证明访问资格，客户端只能表达意图（见 12.1）。
8. **手写循环不可维护**：手写 ReAct 循环里塞满了流式拼接、重试、上下文压缩，改一处崩三处。→ 迁移到 Agent 框架，只保留业务资产（见 15.1）。

---

## 十八、附录

### 附录 A：一次对话的工具调用时序示意

用户对 AI 说："给张三创建一个客户档案。"

```
用户 ──▶ AI
         │
         ├─ 调用「查询工具」：按名称搜客户，确认张三还不存在
         │     ← 结果：0 条命中
         │
         ├─ 调用「确认工具」：提交确认请求
         │     action: 创建客户"张三"
         │     target: 客户表
         │     severity: 中
         │     ← 结果：确认号 #abc123（等待用户确认）
         │
用户看到确认卡片，点"确认执行" ──▶ 系统
         │
         ├─ 调用「创建工具」：创建客户，携带确认号 #abc123
         │     ← 结果：新记录 {id: "xxx", 名称: "张三", ...}
         │
         ├─ 调用「卡片工具」：把记录格式化成结果卡片
         │     ← 结果：卡片渲染数据
         │
用户 ◀── 展示卡片："客户张三创建成功"
```

关键点：确认号和实际执行分两步；确认请求让用户看清"要创建什么"；执行完成后用卡片展示而不是裸 JSON。

一个工具结果的结构示意（概念，非真实数据）：

```json
{
  "success": true,
  "data": { "id": "xxx", "name": "张三", "code": "KH-0001" },
  "error_category": "",
  "suggestion": ""
}
```

一个错误结果的结构示意：

```json
{
  "success": false,
  "error": "此操作需要确认。请先调用确认工具获取确认号，5 分钟内有效；已确认过的可复用确认号直接重试。",
  "error_category": "confirm_required",
  "suggestion": "调用确认工具(action='创建客户张三', target='客户表') 获取确认号后重试"
}
```

### 附录 B：术语表

| 术语 | 含义 |
|------|------|
| 工具 | AI 与数据世界交互的唯一入口，一个可调用的动作单元 |
| 宏工具 | 一个动作一件事的简单工具 |
| 脚本语言工具 | 一次传一串原子指令、系统顺序执行的组合工具 |
| 确认（confirm） | 高危操作前的人机确认门禁，用户拍板后发放确认号 |
| 确认号 | 用户确认后发放的临时凭证，5 分钟有效，可批量复用 |
| 技能（Skill） | 存在数据库里的业务知识文档，教 AI 怎么处理一类任务 |
| 主体（Subject） | 租户/公司的概念，每个主体是独立的业务世界 |
| 操作台 | 传统表单界面，高频作业的辅助入口，与对话并行 |
| FSM/状态机 | 订单/采购等单据的状态流转引擎，AI 不能直接推进——只能创建草稿、删除已取消/已拒绝的终态记录 |
| 上下文降级 | 上下文超限时把内容落盘、按需读回，不中断任务 |
| 错误分类 | 工具错误的机器可读类别，供编排层决策（重试/熔断/审计） |

---

*本文基于启禾 OS · 仓迹 的真实工程实践整理。思路可复用，代码会过时——愿这些设计能帮你在自己的项目里，给 AI 一双安全的手。*
